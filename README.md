# M2M-100-universal-translator
An AI powered and pure client-side web app that translates between 20 major world languages(you can increase them) using a quantized M2M-100 model (ONNX runtime) – all files loaded locally via HTTP server; no API calls, fully offline after setup.
